{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Danh Sách Album</title>
    <style>
        body { font-family: Arial, sans-serif; }
        .album-list { list-style: none; padding: 0; }
        .album-item { margin-bottom: 20px; display: flex; border-bottom: 1px solid #ddd; padding-bottom: 10px; }
        .album-img { width: 150px; height: 150px; margin-right: 20px; }
        .album-details { max-width: 500px; }
        .album-details h2 { margin: 0; font-size: 24px; }
        .album-details p { margin: 5px 0; }
        .artist { margin-top: 10px; }
        .artist-img { width: 50px; height: 50px; border-radius: 50%; margin-right: 10px; }
        .artist-item { display: flex; align-items: center; margin-top: 5px; }
    </style>
</head>
<body>
    <h1>Danh Sách Album</h1>
    <ul class="album-list">
        {{#each albums}}
        <li class="album-item">
            <img class="album-img" src="{{this.img_album}}" alt="Ảnh album {{this.name_album}}">
            <div class="album-details">
                <h2>{{this.name_album}}</h2>
                <p><strong>Năm phát hành:</strong> {{this.releaseDate}}</p>
                <div class="artist">
                    <p><strong>Nghệ sĩ:</strong></p>
                    {{#each this.artist}}
                    <div class="artist-item">
                        <img class="artist-img" src="{{this.img_artist}}" alt="Ảnh nghệ sĩ {{this.name_artist}}">
                        <div>
                            <p><strong>{{this.name_artist}}</strong></p>
                            <p>{{this.bio_artist}}</p>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </li>
        {{/each}}
    </ul>
</body>
</html> --}}
